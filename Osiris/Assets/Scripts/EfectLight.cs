using UnityEngine;

public class EfectLight : MonoBehaviour
{
    /*
    public GameObject myLight;
    public float minIntensity = 0f;
    public float maxIntensity = 1f;
    public float speed = 1f;

    private bool increasing = true;

    void Update()
    {
        // Плавне збільшення та зменшення інтенсивності світла
        if (increasing)
        {
            myLight.intensity += speed * Time.deltaTime;
            if (myLight.intensity >= maxIntensity)
                increasing = false;
        }
        else
        {
            myLight.intensity -= speed * Time.deltaTime;
            if (myLight.intensity <= minIntensity)
                increasing = true;
        }
    }*/
}